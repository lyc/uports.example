prefix=%PREFIX%
exec_prefix=${prefix}
libdir=${exec_prefix}/lib
sharedlibdir=${libdir}
includedir=${prefix}/include

Name: quickjs
Description: QuickJS is a small and embedded Javascript engine.
Version: %VERSION%

Requires:
Libs: -L${libdir}/quickjs -lquickjs -lm -ldl -lpthread
Cflags: -I${includedir}
